declare source_dir;
source_dir = "$$$dir$$$";

declare output_dir;
output_dir = "M:\\Media\\Video\\Super8\\wrk\\1982-skola\\";

VirtualDub.Open(source_dir + "$$$clip$$$.avs");
VirtualDub.video.SetMode(0);
VirtualDub.SaveAVI(output_dir + "$$$clip$$$f.avi");
