declare source_dir;
source_dir = "$$$dir$$$";

declare output_dir;
output_dir = "M:\\Media\\Video\\Super8\\wrk\\1982-skola\\v2\\";

VirtualDub.Open(source_dir + "$$$clip$$$r.avs");
VirtualDub.video.SetMode(0);
VirtualDub.SaveAVI(output_dir + "$$$clip$$$rc.avi");
